// AppVersion.swift
// Single source of truth for the application version.

enum AppVersion {
    static let major = 0
    static let minor = 1
    static let patch = 0
    static let string = "\(major).\(minor).\(patch)"
}
